import React from 'react';
import { Text } from 'react-native-elements';
import { View } from 'react-native';

const Following = () => {
    return (
        <View>
            <Text>Welcome to following</Text>
        </View>
    );
};

export default Following;
